#include <stdio.h>

/**
 * main - print the name 
 * @argc: - count array
 * @argv: - array
 *
 * Return: 0;
 */
int main (int __attribute__((unused))argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
