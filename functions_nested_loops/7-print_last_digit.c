#include "main.h"

/**
 * print_last_digit - print the last digit
 * @digit: - save values
 *
 * Return: 0;
 */
int print_last_digit(int digit)
{
if (digit % 10)
return (digit);
else
return (-digit);
}
