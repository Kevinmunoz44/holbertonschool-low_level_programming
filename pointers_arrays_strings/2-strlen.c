#include "main.h"
#include <stdio.h>

/**
 * _strlen - print the lenght of string
 * @s: - save values in s
 *
 * Return: 0;
 */
int _strlen(char *s[])
{
int len;
for (len = 0; *s[len] != '\0'; len++)
{
_putchar
}
