#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * print_rev - print all in reverse
 * @s: - save values
 *
 * Return: 0;
 */
void print_rev(char *s)
{
int k;
for (k = 0; k <= s; k++)
for (k = k - 1;k[s]; k--)
{
_putchar(k[s]);
}
_putchar('\n');
}
