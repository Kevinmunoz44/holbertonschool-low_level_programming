#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - print the long the character
 * @s: - save values
 *
 * Return: 0;
 */
int _strlen_recursion(char *s)
{
int i;
i = strlen(s);
return (i);
}
