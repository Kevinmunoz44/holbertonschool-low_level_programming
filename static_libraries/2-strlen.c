#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - print the lenght of string
 * @s: - save values in s
 *
 * Return: 0;
 */
int _strlen(char *s)
{
int result;
result = strlen(s);
return (result);
}
